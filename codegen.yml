overwrite: true
schema: "https://gitlab.com/api/graphql"
generates:
  ./graphql.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
config:
  namingConvention:
    enumValues: keep
