overwrite: true
schema: "https://gitlab.com/api/graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
  ./graphql.schema.json:
    plugins:
      - "introspection"
config:
  namingConvention:
    enumValues: keep
